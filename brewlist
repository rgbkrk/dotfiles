# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
# (echo; echo 'eval "$(/opt/homebrew/bin/brew shellenv)"') >> /Users/kyle/.zprofile\neval "$(/opt/homebrew/bin/brew shellenv)"

brew install ack gcc llvm gvim git-lfs python ffmpeg pyenv gpg cmake protobuf deno
